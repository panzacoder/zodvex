node_modules/
tmp/

# Convex deploy secrets (per-developer)
.env.local
_deploy/.env.local

# Transient benchmark output
results/*.log

# Default output location for benchmark runs. results/ proper is
# curated — promote a local run there deliberately, with a name that says
# what it measured.
results/local/

# Generated source staged by the workload benchmark.
_deploy/convex/*
!_deploy/convex/convex.config.ts
