node_modules/
tmp/

# Convex deploy secrets (per-developer)
.env.local
_deploy/.env.local

# Transient harness output
.measure-result.json
results/*.log

# Stale output dirs from the legacy compose/measure harness (removed
# in fbbcebb). Kept ignored in case anyone has them locally.
convex/generated/
convex/composed/
convex/compiled/
