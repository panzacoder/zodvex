node_modules/

# Composed seeds — written into convex/ root by stress-test.ts on every run.
# `_generated/`, `convex.config.ts`, `tsconfig.json`, `README.md` (the only
# checked-in convex/ files) are untouched by the runner; everything else is
# regenerated.
convex/schema.ts
convex/functions.ts
convex/endpoints.ts
convex/models/
convex/endpoints/
convex/summary.json
convex/_zodvex/

.env.local

# Per-machine ceiling cache (push durations vary by network; deployment slug
# is per-developer). The fingerprint key is reproducible but the recorded
# ceilings include host-specific timing — gitignore.
results/ceilings.cache.json
