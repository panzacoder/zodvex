{
  "name": "stress-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "bunx tsc --noEmit -p tsconfig.json",
    "capacity": "bun run capacity/run.ts",
    "memory": "bun run memory/run.mjs"
  },
  "dependencies": {
    "convex": "^1.28.0",
    "convex-helpers": ">=0.1.101-alpha.1",
    "zod": "4.5.4",
    "zodvex": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0"
  }
}
