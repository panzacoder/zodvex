name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  test:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run type check
        run: bun run type-check

      - name: Run example type checks
        run: bun run type-check:examples

      - name: Run tests
        run: bun run test

      - name: Run build
        run: bun run build

      - name: Verify example app codegen is up to date
        run: |
          bun install
          bun run --cwd examples/task-manager generate
          git diff --exit-code examples/task-manager/convex/_zodvex/ || {
            echo ""
            echo "ERROR: Generated files are stale. Run 'cd examples/task-manager && bun x zodvex generate' and commit the result."
            exit 1
          }

      - name: Smoke test task-manager codegen
        working-directory: examples/task-manager
        run: bunx convex codegen

      - name: Smoke test task-manager-mini codegen
        working-directory: examples/task-manager-mini
        run: bunx convex codegen

  lint:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.9

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Run lint
        run: bun run lint
